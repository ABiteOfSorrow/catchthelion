export interface Hello {
    text: string;
}

export const User = {
    name: 'user1'
}

// if use export default, you can export only default type for this file.
export default class A {
    a() {
        
    }
}